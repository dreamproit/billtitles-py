apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "billtitles-py.fullname" . }}-test-connection"
  labels:
    {{- include "billtitles-py.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "billtitles-py.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
